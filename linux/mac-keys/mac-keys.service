[Unit]
Description=Mac keys settings

[Service]
ExecStart=bash -c '/usr/bin/xhost +SI:localuser:root && mac-keys-kill && mac-keys-start'
ExecStop=bash -c 'pkill -f bin/xkeysnail && exit 0'
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical.target
