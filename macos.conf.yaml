- defaults:
    link:
      relink: true
      create: true
      force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    # Zsh config files
    ~/.zsh_os: macos/zsh_os.sh
    # Global gitignore
    ~/.gitignore: macos/gitignore
